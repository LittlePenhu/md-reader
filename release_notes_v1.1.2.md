## MD 阅读器 v1.1.2

本地 Markdown 阅读器：5 套主题、离线渲染、阅读进度记忆。双击即用，纯本地运行，不上传任何数据。

### 功能亮点

- **5 套主题 × 2 种模式**：黛绿新中式 / 书卷暖纸 / 极简 / 紫夜 / OLED 纯黑，深浅色各自成一套配色；默认主题可在设置中修改并持久记忆
- **主页**：最近文件卡片列表（含相对时间）、一键打开、支持拖拽 .md 文件进窗口
- **设置页**：主题卡片直选、深浅色跟随系统、字号滑块、页面宽度、启动行为、清除记录
- **阅读体验**：目录侧边栏（滚动高亮当前章节）、全文搜索（防抖）、阅读进度记忆、外部修改自动刷新（F5 手动刷新）、本地相对路径图片加载（file:// 直载 + base64 兜底）
- **代码**：marked + highlight.js + DOMPurify 渲染库本地化，离线可用；代码块 hover 显示语言标签与一键复制
- **兼容**：UTF-8 / GB18030 / UTF-16 编码自适应、YAML front matter 剥离、大文件保护（>10MB）、外链安全交给系统浏览器

### 使用

下载 `MD阅读器.exe` 双击运行；命令行 `MD阅读器.exe 文件.md` 直接打开指定文件。

### 说明

- 需要 Windows 10/11（内置 Edge WebView2 运行时）
- 源码与构建方式见仓库 README
